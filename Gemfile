# frozen_string_literal: true

source 'https://gem.coop'

# load dependencies from gemspec
gemspec

# Use fork with bug fix for YARD relative markdown links
gem 'yard-relative_markdown_links',
    git: 'https://github.com/znorris/yard-relative_markdown_links.git',
    branch: 'relative-links-sub-dir'
